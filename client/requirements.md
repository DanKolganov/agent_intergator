## Packages
framer-motion | Page transitions and scroll-triggered animations
clsx | Conditional class names merging
tailwind-merge | Tailwind class conflict resolution

## Notes
The application relies on `framer-motion` for polished, premium entry animations.
Assuming standard `@shared/schema` and `@shared/routes` paths based on instructions.
